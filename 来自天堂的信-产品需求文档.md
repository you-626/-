# 《来自天堂的信》产品需求文档(PRD)

**版本**:V1.4(已根据反馈修订:"公开到大厅"开关挪至创建/封存步骤设置,与提前查看开关同步)
**日期**:2026年6月
**文档说明**:本文档用于指导 MVP 阶段的开发,技术选型已结合团队现状(原生微信小程序 + Python 后端)给出建议,可根据实际人力调整。

---

## 1. 项目背景与定位

一句话定位:**一个用文字 / 语音 / 视频写信,设定未来某个时间点才能被打开的"时间胶囊"微信小程序**。

核心情感价值:让用户把"现在想说但还不能说 / 还没到时候说"的话,安心交给时间。

**重要边界与权限模型(开发前必须明确)**:
- 本产品**不涉及任何形式的资金托管、资金代付、虚拟货币提现或转赠**。所有付费行为均为"购买保存时长服务"的一次性消费,通过微信支付直接完成,不在平台内形成可提现、可转赠的余额或虚拟资产。
- **内容查看权限**:
  - **超级管理员**:任意时间均可查看信件内容,仅用于内容安全审核、用户举报处理、法律合规请求响应等场景,不用于日常浏览。
  - **创建者(写信人)本人**:任意时间均可查看自己创建的信件内容。
  - **接收者**:默认仅在送达时间到期后才能查看;创建者可在写信 / 封存时勾选"允许对方提前查看",开启后接收者可不受时间限制随时查看。
- **接收者访问方式**:若收件人设定为"未来的自己",接收者即创建者本人,直接在"我的信箱"中查看;若设定为"想念的人 / 宝宝 / 朋友"等需要真正发给他人的场景,采用**专属链接 / 二维码**分享,接收者点击即可查看,无需注册或登录小程序,降低使用门槛。
- 由于超级管理员具备随时查看权限,**前端文案中"内容只有你知道"等强承诺型表述需要调整**,建议改为"内容经过加密保存,仅在到期后对TA开放"一类更准确的表述,避免与实际权限模型产生认知差异。

---

## 2. 目标用户

| 用户类型 | 典型场景 |
|---|---|
| 自我对话型用户 | 写给"未来的自己",新年计划、阶段性心情记录 |
| 情感纪念型用户 | 给已故或想念的亲友写信,作为情感疏导 |
| 家庭场景用户 | 给"还没出生的宝宝"或孩子的成长节点写信 |
| 仪式感型用户 | 毕业季 / 纪念日 / 求婚等特殊节点的"未来留言" |

---

## 3. 核心功能需求

### 3.1 用户体系

- 微信一键登录(`wx.login` 获取 `openid` 作为用户唯一标识)。
- 用户昵称 / 头像:不强依赖微信头像昵称授权(微信已限制 `wx.getUserProfile` 的强授权弹窗),采用"匿名占位头像 + 用户可自行编辑昵称"的方式,避免授权失败影响核心流程。

### 3.2 写信模块

| 项 | 说明 |
|---|---|
| 输入方式 | 文字(默认) / 语音 / 视频,三选一,Tab 切换 |
| 文字 | **无字数上限**,展示实时字数统计;前端需对超长文本做分段渲染优化,避免卡顿 |
| 语音 | 录制前提供时长选项:**短时(≤3 分钟,免费)/ 长时(≤60 分钟,需解锁)**,与保存时长档位无关(见 3.3 节)。展示录制波形与计时,支持重录、删除,录制中实时提示预估文件大小 |
| 视频 | 时长选项规则与语音一致(**短时 ≤3 分钟免费 / 长时 ≤60 分钟需解锁**),需做客户端压缩后再上传,录制中实时提示预估文件大小与预计上传时长 |
| 寄给 | 预置选项:未来的自己 / 想念的人 / 还没出生的宝宝 / 多年后的朋友(MVP 仅作情感占位,见第 1 章边界说明) |
| 送达时间 | 日期选择器;限制最小送达时间(建议 ≥ 明天)、最大送达时间(建议 ≤ 30 年后),避免无意义的"即时送达"或无限期占用存储 |
| 草稿 | 写到一半退出需保留草稿,**支持云端同步**,换设备 / 重装小程序后草稿不丢失 |
| 封存 | 确认后信件进入"待开启"状态;内容加密存储。查看权限:超级管理员与创建者本人任意时间可查看;接收者默认仅到期后可查看 |
| 提前查看开关 | 写信 / 封存流程中提供"允许接收者提前查看"开关(默认关闭),由创建者自行决定是否解除时间限制 |
| 公开到大厅开关 | 写信 / 封存流程中**同步**提供"公开到大厅"开关(默认关闭)。这里设置的只是创建者的意向,实际是否出现在大厅还需满足 3.7 节的时间条件;生效前创建者可随时回到信件详情页修改此设置 |
| 内容安全 | 文字 / 语音 / 视频均需接入微信内容安全检测接口(`msgSecCheck` / `mediaCheckAsync`),这是小程序过审与合规运营的硬性要求 |

### 3.3 分量(定价)模块

**保存时长定价**(决定信件保留多久,与录制时长无关):

| 保存时长 | 价格 |
|---|---|
| 30 天内 | 免费 |
| 1 年内 | ¥2.9 |
| 1–3 年 | ¥6.9 |
| 3–10 年 | ¥14.9 |
| 10 年以上 / 人生节点 | ¥29.9 |

**媒体录制时长**(与上面的保存时长档位互相独立,单独解锁):

| 录制时长档位 | 价格 | 说明 |
|---|---|---|
| 短时(≤3 分钟) | 免费 | 默认档位,任意保存时长档位下均可使用 |
| 长时(≤60 分钟) | +¥9.9 / 封 | 需单独解锁,**按信件计费**(不是账号级一次性解锁);未解锁时录制界面只提供短时选项 |

- 支付方式:微信支付(小程序支付),一次性扣款,**不形成平台内可提现 / 可转赠的余额或虚拟货币**。
- 免费档限制:建议同一用户同时存在的"待开启免费信"不超过 3 封,防止滥用存储资源。
- **价格说明**:长时录制解锁的 ¥9.9 是按存储成本粗估给出的建议值(60 分钟压缩视频约 400–900MB,1 小时音频约 30–60MB),具体数字建议结合实际云存储报价核算后再确定,这里先按建议值写入文档,方便后续直接调整。

### 3.4 我的信箱模块

- 三个分类:**草稿 / 待开启(显示倒计时) / 已开启**。
- 列表项展示:寄给谁、信件类型图标(文字 / 语音 / 视频)、送达日期、剩余时间。
- 已开启的信可重新查看,不可编辑。

### 3.5 到期与开启模块

- 服务端定时任务每日扫描到期信件,通过微信订阅消息推送提醒。
- 开启时复刻"封存"时的仪式动效(沿用治愈系视觉,如爱心 / 火漆开启动画),强化情感仪式感。

### 3.6 管理端(运营后台)

- **定位**:供超级管理员处理内容安全审核、用户举报、法律合规请求等场景使用,**不用于日常浏览用户信件**。
- **权限**:仅超级管理员账号可登录,可检索 / 查看任意信件内容(含未到期信件)。
- **审计要求**:每一次管理端查看信件内容的行为,均需记录操作人、操作时间、信件 ID,形成审计日志,可供事后追溯,防止权限被滥用。
- MVP 阶段可先做成一个简单的内部工具(基于 FastAPI 接口 + 简单鉴权的内部页面即可),不必投入资源做成完整可视化后台,优先保证"可审核、可追溯"这两个核心能力。

### 3.7 大厅(公开广场)模块

- **触发条件**:"公开到大厅"的开关在**创建 / 封存信件时**就提供给创建者选择(默认关闭),与"允许提前查看"是同一步里的两个独立选项,不需要等信件开启后再回来单独操作。即便创建者当时选择了"公开",实际生效仍需等到**送达时间(`deliver_at`)过后满 10 天**才会真正出现在大厅——这是为了在情绪最浓的当下之外,留出一段冷静期。创建者在公开生效前,可随时回到信件详情页修改这个设置(包括取消公开)。
- **展示**:分享后的信件出现在"大厅"公开列表中,所有用户可浏览、评论、讨论。
- **身份展示**:建议默认匿名或仅显示用户自定义昵称,不强制关联真实头像 / 微信身份,降低用户分享顾虑。
- **评论与内容安全**:评论内容需接入内容安全检测(`msgSecCheck`),并提供举报 / 拉黑机制——开放评论区是微信小程序内容审核的重点关注对象,这两项是上线前的硬性要求,不可省略。
- **管理**:创建者可随时将已分享的信件从大厅下架(取消公开);超级管理员可对违规评论 / 信件做隐藏或删除处理,处理记录同样计入审计日志。

---

## 4. 非功能需求

- **安全**:信件内容(文字 / 语音 / 视频)需加密存储,按第 3.2 / 3.6 节权限模型做访问控制(超级管理员 / 创建者随时可访问,接收者按到期时间或"提前查看"开关控制访问)。管理端的所有查看行为需记录审计日志(操作人、时间、信件 ID),避免权限被滥用。
- **合规**:支付走标准微信支付通道,不自建资金池、不做用户间转账;信件内容与大厅评论均需接入内容安全检测,不可跳过;开放评论区需配套举报 / 拉黑机制。
- **性能**:语音 / 视频最长可达 60 分钟,上传需做客户端压缩 + **分片 / 断点续传**,并提供清晰的上传进度展示,避免低端机型或弱网环境下长时间录制后上传失败导致内容丢失。
- **可用性**:核心交互(写信、选分量、查看信箱、大厅浏览)需在弱网环境下有合理的 loading / 失败重试提示。

---

## 5. 视觉与交互规范(治愈系设计系统)

延续此前已确认的治愈风格视觉稿,作为前端开发的设计基准:

**色板**

| Token | 色值 | 用途 |
|---|---|---|
| `--sky-a / --sky-b / --sky-c` | `#EFE3F7` → `#FCE4D6` → `#FFF6E8` | 页面背景渐变(淡紫 → 桃橙 → 奶油白) |
| `--paper` | `#FFFAF1` | 信纸卡片背景 |
| `--paper-shadow` | `#F1E4D2` | 卡片分隔线 / 弱阴影 |
| `--ink` | `#5B4A42` | 主文字 |
| `--ink-soft` | `#9C8A7E` | 次要文字 |
| `--coral / --coral-light / --coral-deep` | `#F2967D / #FAB59E / #D9785C` | 主操作色(封存按钮、选中态、强调) |
| `--gold` | `#E3B873` | 标签、计数点、辅助强调 |

**字体**:标题使用圆润手写感字体(如 ZCOOL KuaiLe),正文 / 数值使用 Noto Serif SC,UI 文案使用 Noto Sans SC。

**图标规范(强制要求)**:
- **禁止使用 emoji 作为界面图标**(emoji 在不同手机厂商渲染差异大,且与治愈系视觉风格不统一)。
- 统一使用 **Vant Weapp 组件库自带的 Icon 图标**(线性风格),不再额外自建 iconfont 图标库,降低维护成本。
- 图标配色跟随 `--ink` 或 `--coral-deep`,与组件库整体主题色覆盖保持一致(见下方组件库主题定制说明)。

**云朵 / 信纸 / 火漆封印**等装饰元素延续已确认稿件实现,不在此重复列出代码细节。

---

## 6. 技术架构

### 6.1 前端:原生微信小程序

- **不使用 Taro / uniapp 等跨端框架**,采用原生 WXML + WXSS + JS(或 TS)开发,以获得最佳的小程序原生体验和最小的运行时损耗。
- **UI 组件库**:可选用 [Vant Weapp](https://github.com/youzan/vant-weapp) 或微信官方 [WeUI](https://github.com/Tencent/weui-wxss) 作为基础组件(按钮、弹层、表单控件等),两者均支持通过覆盖 SCSS / CSS 自定义属性来替换默认主题色——**必须在引入组件库后,立即按第 5 章色板覆盖默认配色**,不能直接使用组件库默认的蓝色 / 绿色系主题。
- 自定义交互元素(写信卡片、封存火漆动效、分量选择卡片、云朵背景等)按已确认的设计稿用原生 WXML/WXSS 还原,这部分不依赖组件库。

### 6.2 后端:Python

推荐框架:**FastAPI**

理由:
- 原生支持异步(`async/await`),适合小程序这种"短请求、高并发"的接口模式(写信、查询信箱列表、查询分量价格等);
- 基于 Pydantic 做请求 / 响应数据校验,配合自动生成的 Swagger 文档,便于前后端协作与后续维护;
- 相比 Django 更轻量,本项目核心是清晰的 API 服务,不需要 Django 自带的重型 Admin 后台;如果后续需要运营 / 客服后台(查看举报、内容审核工单等),可以再单独引入一个轻量管理后台,不必绑死在同一框架里。

如果团队对 Django 更熟悉、且预期后续需要快速搭建后台管理系统,Django + Django REST Framework 也是合理的备选方案,只是上手和迭代速度通常不如 FastAPI 快。

**部署方式:微信云托管**

确定采用[微信云托管](https://cloud.weixin.qq.com/)而非自建服务器:
- 将 FastAPI 服务打包为 Docker 镜像,直接部署到云托管,无需自己处理服务器、域名备案、HTTPS 证书;
- 小程序端通过 `wx.cloud.callContainer` 直连后端容器,免去自己维护鉴权和跨域配置;
- 云托管自带的云数据库、存储、定时触发器可直接复用,减少额外引入第三方账号的成本。

**配套技术选型建议**:

| 模块 | 建议方案 |
|---|---|
| 数据库 | MySQL(微信云托管的云数据库目前以 MySQL 为主,优先选择以保证兼容性) |
| 对象存储(语音/视频) | 微信云托管自带的存储能力(底层基于腾讯云 COS),无需单独开通账号 |
| 定时任务(到期扫描/推送) | 微信云托管的"定时触发器"功能,无需额外引入 APScheduler / Celery + Redis |
| 内容安全检测 | 微信开放接口 `msgSecCheck`(文本)/ `mediaCheckAsync`(音视频) |
| 支付 | 微信支付小程序支付(JSAPI),标准接入,不自建余额系统 |

### 6.3 核心数据模型(示意)

**users**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| openid | varchar | 微信用户唯一标识 |
| nickname | varchar | 用户昵称(可编辑) |
| created_at | timestamp | 注册时间 |

**letters**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| user_id | bigint | 创建者 |
| type | enum | text / audio / video |
| content_key | varchar | 加密内容的存储引用(文本密文 或 媒体文件 Key) |
| recipient_label | varchar | 收件人占位称呼 |
| deliver_at | timestamp | 送达时间 |
| allow_early_view | boolean | 创建者是否允许接收者提前查看,默认 false |
| share_token | varchar | 接收者访问用的专属链接 / 二维码令牌(收件人非本人时使用) |
| status | enum | draft / sealed / opened |
| tier | varchar | 对应分量档位 |
| is_public | boolean | 创建者在创建 / 封存时设置的"公开到大厅"意向(默认 false),生效前可随时修改;实际是否展示在大厅由 `is_public = true` 且已过 `deliver_at + 10 天` 且 `status = opened` 三个条件共同决定 |
| media_unlocked | boolean | 是否已解锁长时录制(≤60 分钟),默认 false,按信件计费 |
| media_duration_seconds | int | 实际录制时长(秒),用于校验是否超出短时限制 |
| created_at / sealed_at / opened_at | timestamp | 各状态时间戳 |

**admin_access_logs**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| admin_id | bigint | 操作的超级管理员 |
| letter_id | bigint | 被查看的信件 |
| viewed_at | timestamp | 查看时间 |

**orders**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| user_id | bigint | 下单用户 |
| letter_id | bigint | 关联信件 |
| item_type | enum | storage(保存时长) / media_unlock(长时录制解锁) |
| amount | decimal | 实付金额 |
| wx_transaction_id | varchar | 微信支付交易号 |
| status | enum | pending / paid / refunded |
| created_at | timestamp | 下单时间 |

**comments**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| letter_id | bigint | 所属信件(需为已公开到大厅的信件) |
| user_id | bigint | 评论者 |
| content | text | 评论内容 |
| status | enum | normal / hidden / reported |
| created_at | timestamp | 评论时间 |

**reports**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| target_type | enum | letter / comment |
| target_id | bigint | 被举报的信件或评论 ID |
| reporter_id | bigint | 举报人 |
| reason | varchar | 举报原因 |
| status | enum | pending / resolved |
| created_at | timestamp | 举报时间 |

---

## 7. 里程碑规划

**MVP(V1)**
- 微信登录
- 写信(仅文字 + 送达时间设置),支持草稿云端同步
- 免费档 + 付费时长档(暂不含语音/视频附加)
- "允许接收者提前查看"开关
- 接收者通过专属链接 / 二维码查看(非"写给自己"场景)
- 我的信箱(待开启 / 已开启列表)
- 到期订阅消息提醒 + 开启
- 管理端基础查看 + 审计日志(内部工具即可,见 3.6 节)

**V1.1**
- 语音 / 视频写信(短时 3 分钟免费 / 长时 60 分钟需解锁付费,分片上传 + 断点续传)
- 大厅(公开广场):分享开关(送达时间 +10 天后开放)、评论、内容安全检测、举报 / 拉黑机制

**V2(暂不在本期范围内)**
- 实体明信片邮寄增值服务
- 节日 / 医院 / 学校 / 企业 B 端合作定制

---

## 8. 已确认决策

此前版本中标注为"待确认"的事项,现已根据反馈确认如下:

- 免费档同时存在的"待开启信"数量上限:**3 封**;
- 草稿:**需要云端同步**(已更新至 3.2 节及第 7 章 MVP 范围);
- 图标:**采用 Vant Weapp 组件库自带图标**,不再自建 iconfont(已更新至第 5 章)。

(语音 / 视频录制时长已调整为与保存时长档位解耦的独立解锁项——短时 ≤3 分钟免费,长时 ≤60 分钟需单独付费解锁,详见 3.2 / 3.3 节,此前"免费档 / 付费档绑定时长"的版本已不再适用。)
------------------------------------------------------------------------
# 《来自天堂的信》项目融资计划书

**融资金额**:人民币 30 万元
**资金用途**:产品开发上线 + 初期市场启动
**当前阶段**:产品设计稿与完整需求文档已完成,即将进入开发阶段

---

## 一、项目简介

《来自天堂的信》是一款微信小程序,核心玩法是用文字 / 语音 / 视频写一封信,设定未来某个时间点(几个月后、几年后、孩子18岁那天……)才能被打开,把"现在想说但还不能说"的话安心交给时间。

## 二、市场痛点与机会

情感记录类产品长期存在真实需求——人需要一个安全的出口,把当下无法说出口、或者想留给未来的话先放下来。同类"时间胶囊"赛道已有项目跑出规模并拿到天使轮融资,验证了这个方向有真实的用户需求和资本认可度,但市场上仍缺少视觉和情感体验做得足够细致、定价模式足够清晰的产品,这正是我们的切入点。

## 三、产品与当前进展

- **核心功能**:文字 / 语音 / 视频写信、按保存时长分级定价、长时录制解锁、信箱管理、到期提醒、公开广场(用户可选择是否分享、并设有冷静期机制)。
- **已完成**:全套治愈系视觉设计稿(写信页、收费页等)、完整产品需求文档(PRD)、技术方案选型确定(原生微信小程序 + Python FastAPI + 微信云托管)。
- **下一步**:启动开发,目标在数月内完成 MVP 并上线。

## 四、商业模式

| 收费项 | 定价 |
|---|---|
| 保存时长(30天 / 1年 / 1-3年 / 3-10年 / 10年以上) | 免费 — ¥29.9 |
| 长时录制解锁(≤60分钟,默认短时3分钟免费) | ¥9.9 / 封 |

未来可拓展方向:实体明信片邮寄增值服务、节日 / 医院 / 学校 / 企业 B 端定制合作。所有收费均为一次性消费,通过微信支付直接完成,不涉及资金托管或用户间转账。

## 五、目标用户

写给未来自己的自我对话型用户、写给逝者或想念之人的情感纪念型用户、写给宝宝或孩子成长节点的家庭用户,以及毕业季 / 纪念日等仪式感场景的用户。

## 六、财务预估(区间估算,非承诺性预测)

实际表现高度依赖推广投入和内容传播效果,以下为粗略量级参考,不构成业绩承诺:

| 推广力度 | 注册用户(年内) | 年收入 |
|---|---|---|
| 保守(自然传播为主) | 3万–8万 | ¥1万–5万 |
| 中等(适度投流+种草) | 20万–50万 | ¥20万–60万 |

## 七、资金用途明细

| 用途 | 金额 |
|---|---|
| 产品开发(前后端开发、测试、上线审核) | ¥70,000 |
| 内容与推广启动(内容制作 + 初期精准投流测试) | ¥40,000 |
| 云服务与基础设施(云托管、存储、内容安全接口、企业认证等首年成本) | ¥15,000 |
| 法务与合规咨询(用户协议、隐私政策、支付合规审查) | ¥10,000 |
| 运营周转 / 应急储备 | ¥165,000 |
| **合计** | **¥300,000** |
 